
#include <iostream>

using namespace std;

int main(int argc, char const *argv[]) {
  // TODO: Declare and initialize a const integer named counter max (max for
  // maximum). Initialize it to 10.
  cout << "With a for loop\n";
  // TODO: Write a for loop which prints out the numbers 0 through 9 with each
  // number on a line by itself. Use the previously defined constant to control
  // the loop.

  cout << "\nWith a while loop\n";
  // TODO: Write a while loop which prints out the numbers 0 through 9 with each
  // number on a line by itself. Use the previously defined constant to control
  // the loop.

  cout << "\nWith a do-while loop\n";
  // TODO: Write a do-while loop which prints out the numbers 0 through 9 with
  // each number on a line by itself. Use the previously defined constant to
  // control the loop.

  return 0;
}